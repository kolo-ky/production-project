import React from 'react';
import classes from './foo.module.scss';

export const Foo = () => {
    return (
        <div className={classes.container}>
            Hello world!
        </div>
    );
};
