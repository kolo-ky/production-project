import { FC } from 'react';

interface INotFoundPageProps {
    className?: string
}

export const NotFoundPage: FC<INotFoundPageProps> = () => {
    return (
        <div>
            NotFoundPage
        </div>
    );
};